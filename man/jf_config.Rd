% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{jf_config}
\alias{jf_config}
\title{Point journalfig at a project override file}
\usage{
jf_config(path = NULL)
}
\arguments{
\item{path}{Path to an override JSON, or \code{NULL} to look for
\code{journal_fig.json} in the working directory and then in \verb{R/}.}
}
\value{
The merged configuration, invisibly.
}
\description{
Geometry and type are lab-wide and ship with the package. The font registry
is per-machine, because licensed families are installed on some machines and
not others, so only the collaborator-safe face ships. A project overrides by
putting \code{journal_fig.json} at its root, naming only what differs; it is
deep-merged over the defaults.
}
\details{
Call with no argument to reload, or to let it find the file itself.
}
\examples{
\dontrun{
jf_config("journal_fig.json")
}
}
\seealso{
\code{\link[=jf]{jf()}} to read it, \code{\link[=jf_font]{jf_font()}} to choose a face.
}
