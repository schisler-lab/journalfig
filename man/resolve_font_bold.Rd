% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fonts.R
\name{resolve_font_bold}
\alias{resolve_font_bold}
\title{The family and face to use for bold text}
\usage{
resolve_font_bold(font = jf_font(), allow_fallback = FALSE)
}
\arguments{
\item{font}{A key in the font registry, or a family name outright.}

\item{allow_fallback}{Permit the safe face if \code{font} is not installed.}
}
\value{
A list of \code{family} and \code{face}, ready to hand to
\code{\link[ggplot2:element_text]{ggplot2::element_text()}}.
}
\description{
Some families carry no internal bold and use a separate weight as their
bold. Concourse is one: its three lighter weights use weight 6, so asking
ggplot for \code{face = "bold"} gets a synthesized, smeared fake. Where the
configuration's \code{bold_for} names a real bold partner, that family is
returned with the face left alone. Weights in a duplexed family have
identical character widths, so swapping one in never reflows anything.
}
