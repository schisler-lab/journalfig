% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fonts.R
\name{resolve_font}
\alias{resolve_font}
\title{Resolve a font key, or a family name, to an installed family}
\usage{
resolve_font(font = jf_font(), allow_fallback = FALSE, tabular = TRUE)
}
\arguments{
\item{font}{A key in the font registry, or a family name outright.
Defaults to the active face, see \code{\link[=jf_font]{jf_font()}}.}

\item{allow_fallback}{Permit the safe face if \code{font} is not installed.
\code{FALSE}, the default, stops instead.}

\item{tabular}{Ask for uniform-width digits. Right for a y axis, where
numbers stack and the eye reads them as a column. Wrong in running text,
where it pads narrow digits out and reads gappy.}
}
\value{
An installed family name.
}
\description{
Never substitutes silently. A missing font stops the render, because a quiet
fall back to Helvetica is not noticed until proof.
}
\details{
With \code{tabular = TRUE} there are three routes and which applies depends on
the foundry. A real installed sibling family (\code{"Concourse 3 Tab"}) is
preferred, because the name resolves through the operating system and every
device honours it. A family whose digits are already uniform is returned
unchanged. Only as a last resort is a \code{tnum} variant registered, with a
warning, because a registered variant is a systemfonts alias that svglite
honours and cairo_pdf does not: the SVG comes out perfect and the PDF
silently substitutes.

Licensed faces are safe here for work owned end to end, because the pipeline
outlines in Illustrator before anything ships and the font itself never
travels. A collaborator who must edit the live file needs the safe face,
which is what \code{font = "safe"} is for.
}
\seealso{
\code{\link[=font_report]{font_report()}} to see which route a face takes.
}
