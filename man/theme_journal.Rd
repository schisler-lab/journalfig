% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_journal}
\alias{theme_journal}
\title{The journal theme}
\usage{
theme_journal(
  width = "1col",
  font = jf_font(),
  ticks = c("none", "x", "y", "both"),
  allow_fallback = FALSE
)
}
\arguments{
\item{width}{Figure width key, see \code{\link[=fig_width]{fig_width()}}.}

\item{font}{Face to use, defaulting to the active one. Set it with
\code{\link[=jf_font]{jf_font()}} before building anything rather than passing it here.}

\item{ticks}{Which axes carry tick marks. Ticks indicate continuous data, so
a categorical axis gets none.}

\item{allow_fallback}{Permit the safe face if \code{font} is not installed.}
}
\value{
A ggplot theme.
}
\description{
\code{width} is the FIGURE key, not a panel's. Every panel in one figure is given
the same theme, so type size is uniform across the figure rather than
varying panel by panel.
}
\details{
Structure is grey so it recedes and content is black: axis lines and ticks
grey, tick labels black regular, axis titles black bold. The y axis gets
tabular digits, because that is where numbers stack and the eye reads them
as a column. Everything else stays proportional, since tabular pads narrow
digits out and makes a string like \code{"rho = +0.50, p < 0.001"} read gappy.

The theme owns structure colour only. Data colour belongs to the project
palette and this package never touches it.
}
\seealso{
\code{\link[=annotate_stat]{annotate_stat()}}, \code{\link[=save_journal]{save_journal()}}
}
