% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_journal}
\alias{save_journal}
\title{Save a finished figure at final print size, with live text}
\usage{
save_journal(
  plot,
  name,
  width = "1col",
  height_mm,
  fmt = c("svg", "pdf", "png"),
  outdir = "figures",
  font = jf_font(),
  check = TRUE
)
}
\arguments{
\item{plot}{A ggplot or patchwork composition.}

\item{name}{File name without extension.}

\item{width}{Figure width key, see \code{\link[=fig_width]{fig_width()}}.}

\item{height_mm}{Height in millimetres. Capped by the configuration's page
maximum.}

\item{fmt}{Which formats to write.}

\item{outdir}{Directory to write into, created if needed.}

\item{font}{Face to check against, defaulting to the active one.}

\item{check}{Reopen and verify what was written.}
}
\value{
The paths written, invisibly.
}
\description{
Exports once, at the size the figure will print. There is no scale factor
and there is nothing to shrink afterwards.
}
\details{
SVG is the Illustrator handoff, written by svglite with
\code{fix_text_size = FALSE}. That flag is not optional: by default every string
is pinned to a fixed \code{textLength}, and editing that text in Illustrator then
stretches or squeezes it back to the old width.

PDF via cairo embeds a font SUBSET. The text is live, but you are editing
against a subset, so typing a glyph it does not carry is where people get
stuck. Use it when a PDF is required and prefer SVG.

PNG is work in progress only and never a deliverable.

With \code{check = TRUE} the written files are reopened and asserted against the
spec, so a figure that misses it fails here rather than at proof.
}
\seealso{
\code{\link[=check_journal]{check_journal()}}, \code{\link[=check_pdf_fonts]{check_pdf_fonts()}}
}
