% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placeholder.R
\name{panel_placeholder}
\alias{panel_placeholder}
\title{A correctly sized empty slot for art that code cannot generate}
\usage{
panel_placeholder(
  note = "art to be placed",
  font = jf_font(),
  allow_fallback = FALSE
)
}
\arguments{
\item{note}{What goes here, drawn inside the box.}

\item{font}{Face to use, defaulting to the active one.}

\item{allow_fallback}{Permit the safe face if \code{font} is not installed.}
}
\value{
A ggplot, marked so \code{\link[=save_journal]{save_journal()}} can count it.
}
\description{
A design schematic, a pathway diagram, a microscopy montage. It composes
like any other panel, so the figure carries its true final dimensions from
the first day rather than from assembly, and Illustrator gets an exact box
to drop art into.
}
\details{
\code{\link[=save_journal]{save_journal()}} warns on every export that still contains one, so an
unfilled slot cannot quietly become the submitted version.
}
