% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{jf_font}
\alias{jf_font}
\title{Get or set the face this figure is using}
\usage{
jf_font(font = NULL)
}
\arguments{
\item{font}{A key in the configuration's font registry, or a family name
outright. Omit to read the active face rather than set it.}
}
\value{
The active face. Invisibly when setting.
}
\description{
The face is a decision about a FIGURE, not about a call, so it is set once
and every function in the package defaults to it.
}
\details{
Call it FIRST, before building any layer. It is deliberately not a side
effect of \code{\link[=theme_journal]{theme_journal()}}. R evaluates a default argument lazily, at the
moment the function first needs it, so in \code{p + annotate_stat(...) + theme_journal(...)} the annotation resolves its font before the theme is
ever evaluated. Anything that sets the face while a plot is being built
arrives too late for the layers next to it, and the figure comes out mixed
according to the order somebody happened to type.

Skipping it is not loud. Every call site independently falls back to the
safe face, so a figure comes out mostly in the default with a few strings in
the family you thought you had asked for. \code{\link[=check_journal]{check_journal()}} and
\code{\link[=check_pdf_fonts]{check_pdf_fonts()}} catch that afterwards; this prevents it.
}
\examples{
\dontrun{
jf_font("concourse")
p <- ggplot(df, aes(x, y)) + annotate_stat("rho = +0.50") + theme_journal("2col")
}
}
\seealso{
\code{\link[=resolve_font]{resolve_font()}}, \code{\link[=font_report]{font_report()}}
}
